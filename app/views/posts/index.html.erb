<h1>All posts:</h1>
<table>
  <tr>
    <th>Title</th>
    <th>Author</th>
  </tr>
  <% @posts.each do |p| %>
    <tr>
      <td><%= p.title %></td>

      <% if logged_in? %>
        <td><%= User.find_by(id: p.user_id).name %></td>
      <% end %>

    </tr>
  <% end %>
</table>
